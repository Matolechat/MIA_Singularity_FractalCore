from datetime import datetime

def phi_genesis():
    now = datetime.utcnow().isoformat()
    print(f"[ϕ-Genesis] Seed initialized at: {now}")
    return {"ϕ": "true", "timestamp": now}

if __name__ == "__main__":
    print("[ϕ] Launching genesis seed...")
    seed = phi_genesis()
    print(f"[ϕ] Seed content: {seed}")
